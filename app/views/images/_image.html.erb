<div class="image">
	<%= link_to image_tag(image.image_url(:thumb)), image if image.image? %>
	<div class="name"><%= image.name %></div>
	<div class="actions">
		<%= link_to "edit", edit_image_path(image) %> |
		<%= link_to "remove", image, :confirm => 'Are you sure?', :method => :delete %>
	</div>
</div>
