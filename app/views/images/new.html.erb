<h1>New Image</h1>

<%= form_for([current_user, @image]) do |f| %>
	<div class="field">
			<%= f.label :file %><br>
			<%= f.attachment_field :file %>
	</div>
	<div class="actions">
		<%= f.submit %>
	</div>
<% end %>

<%# render 'form' %>

<%= link_to 'Back', :back %>
