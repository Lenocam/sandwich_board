<div id="dashboard">
	<p id="notice"><%= notice %></p>
	<h3><%= current_user.name %>'s Galleries</h3>

	<% if current_user.galleries.any? %>
	<div class="row">
		<div class="flex-images">
			<% current_user.galleries.each do |gallery| %>
			<li><%=link_to gallery.title, gallery %></li>
			<div class="item floating-box"><%= link_to attachment_image_tag(gallery.images.first, :file), gallery %></div>
			<% end %>
		</div>
	</div>
	<% end %>
</div>
