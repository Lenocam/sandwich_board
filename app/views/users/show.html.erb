<p id="notice"><%= notice %></p>

<h3><%= @user.name %> Dashboard</h3>

<% if @user.galleries.any? %>
	<ul>
		<% @user.galleries.each do |galleries| %>
		<li><%= link_to galleries.title, galleries %></li>
		<% end %>
	</ul>

<% end %>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
