<p id="notice"><%= notice %></p>

<h1>Listing Users</h1>
	<%= will_paginate %>
<table>
	<thead>
		<tr>
			<th colspan="3"></th>
		</tr>
	</thead>
	<tbody class="users">
		<%= render @users %>
		<td><%= will_paginate %></td>
	</tbody>

<%= link_to 'New User', new_user_path %>
